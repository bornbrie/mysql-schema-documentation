
const Database = () => {
    name = "",
    tables = []
}

const Table = () => {
    name = "",
    columns = []
}

const Columns = () => {
    name = "",
    comment = ""
}

export default { Database, Table, Columns };